import { NgMatrixDirective } from './ng-matrix.directive';

describe('NgMatrixDirective', () => {
  it('should create an instance', () => {
    const directive = new NgMatrixDirective();
    expect(directive).toBeTruthy();
  });
});
